pcm.!default {
  type asym
  capture.pcm "mic"
  playback.pcm "speaker"
}

pcm.mic {
  type plug
  slave {
    pcm "hw:Device"
  }
}

pcm.speaker {
  type plug
  slave {
    rate 16000
    pcm "hw:ALSA"
  }
}
